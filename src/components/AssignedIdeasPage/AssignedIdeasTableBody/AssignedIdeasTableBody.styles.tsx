import { styled, TableRow } from "@mui/material"

const S = {
  TableRow: styled(TableRow)`
    .MuiTableCell-root {
      border-bottom: none;
      padding-left: 30px;
    }
  `,
}

export default S
